namespace DailyGrid.Core.Services;

// 用于聚合多个 delta 相关的 service 
// 先解包，然后一次输入用于 update dailyrecord

public class deltaAddManagement
{
    // 每天应该 new 一个 新的 dailyrecord 
    // 然后每 0.5 s 甚至更短应该接收一个 platform 传过来的 dto package 
    // 解包
    // 把 int or double 等 raw value 直接存入
    



}